bool alphabetSubsequence(string i) {
    string s = i,t = i;
    sort(s.begin(),s.end());
    if(distance(s.begin(), unique(s.begin(),s.end())) != s.size())  return 0;
    sort(t.begin(),t.end());
    if(t != i)    return 0;
    return 1;
}
