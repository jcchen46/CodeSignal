bool areIsomorphic(vector<vector<int>> a1, vector<vector<int>> a2) {
    if(a1.size()!= a2.size())   return 0;
    else{
        for(int i = 0; i < a1.size(); i++){
            if(a1[i].size() != a2[i].size())    return 0;
        }
    }
    return 1;
}
