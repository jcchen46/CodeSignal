vector<string> o, allLongestStrings(vector<string> m) {
    int s = 0;
    for(int i = 0; i < m.size(); i++){
        if(m[i].size() > s) s = m[i].size();
    }
    for(int i = 0; i < m.size(); i++){
        if(m[i].size() == s) o.push_back(m[i]);
    }
    return o;
}
